- hosts: localhost
  become: true
  tasks:
  - name: Install htop
    apt: name=htop

  - name: Install git
    apt: name=git

  - include: tasks/gnome.yml
  - include: tasks/oracle_java_8.yml

  -  script: tasks/netbeans.sh
